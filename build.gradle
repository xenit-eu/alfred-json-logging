subprojects {
    description = "Alfresco json logging"

    group = 'eu.xenit.alfred-json-logging'
    version = "0.0.1"

    repositories {
        mavenLocal()
        jcenter()
    }
}

wrapper {
    distributionType = Wrapper.DistributionType.ALL
    gradleVersion = '4.10.2'
}
